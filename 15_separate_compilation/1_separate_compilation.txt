1. 분할 컴파일
    1-1. 각각의 파일을 gcc -c 파일이름.c
        => 각각의 파일을 목적파일로 만드는 명령어입니다.
    1-2. gcc 목적파일.o 목적파일.o -o 통합된_파일_이름
        => 링커를 통해 링킹작업을 시작해서 하나의 실행 파일로 만듭니다.
        => 이제 통합된 파일 이름으로 실행하면 각각의 파일의 합일되어 실행파일이 됩니다.